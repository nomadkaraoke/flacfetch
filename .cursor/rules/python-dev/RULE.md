---
description: "Python development conventions for the flacfetch project"
alwaysApply: true
---

# Python Development

- Python 3.10+ required
- Use `venv/` for the main application virtual environment
- Use `infrastructure/venv/` for Pulumi/infrastructure work
- Run tests with `pytest`
- Format code with `ruff`

